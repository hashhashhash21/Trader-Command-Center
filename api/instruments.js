const H={'Content-Type':'application/json','X-Content-Type-Options':'nosniff'};const {listInstruments}=require('../lib/instruments');
module.exports=async function(req,res){Object.entries(H).forEach(([k,v])=>res.setHeader(k,v));res.setHeader('Cache-Control','public, max-age=60');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});return res.status(200).json({version:'v75',instruments:listInstruments(),note:'Validation eligibility is explicit per instrument. Spot and US ETF symbols never enter the futures calibration pipeline.'})};