const H={'Content-Type':'application/json','X-Content-Type-Options':'nosniff'};
const U=()=>process.env.SUPABASE_URL,K=()=>process.env.SUPABASE_SERVICE_ROLE_KEY;
const SUPPORTED=['BTCUSDT','ETHUSDT'];
async function db(path){if(!U()||!K())return{configured:false,data:null};const c=new AbortController(),t=setTimeout(()=>c.abort(),5000);try{const r=await fetch(`${U()}/rest/v1/${path}`,{signal:c.signal,headers:{apikey:K(),Authorization:`Bearer ${K()}`,'Content-Type':'application/json'}}),text=await r.text();let data=null;try{data=text?JSON.parse(text):null}catch{}if(!r.ok)throw new Error(`DB ${r.status}`);return{configured:true,data}}finally{clearTimeout(t)}}
module.exports=async function(req,res){Object.entries(H).forEach(([k,v])=>res.setHeader(k,v));res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const symbol=String(req.query?.symbol||'BTCUSDT').toUpperCase();if(!SUPPORTED.includes(symbol))return res.status(400).json({error:'unsupported symbol',supported:SUPPORTED});try{const got=await db(`tcc_observations?symbol=eq.${symbol}&select=symbol,observed_at,price,state,evidence_score,quality_score,regime,mtf,derivatives,orderflow,provenance&order=observed_at.desc&limit=1`);if(!got.configured)return res.status(200).json({configured:false,symbol});const row=got.data?.[0]||null;if(!row)return res.status(200).json({configured:true,symbol,available:false});const ageMs=Math.max(0,Date.now()-new Date(row.observed_at).getTime()),fresh=ageMs<=12*60e3,valid=row.provenance?.dataClass==='futures-validated';return res.status(200).json({configured:true,available:true,version:'v73',engine:'canonical-server',symbol,fresh,ageMs,validFuturesProvenance:!!valid,snapshot:row,guardrail:'Canonical snapshot is server-collected market evidence. It is descriptive and not a trade instruction.'})}catch{return res.status(500).json({error:'snapshot read failed'})}};